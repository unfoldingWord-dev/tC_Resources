import { getUrl } from "../helpers/door43ApiHelper";

describe('Door43 API Helper', () => {
    test('getUrl with valid url', () => {
        expect(false).toBeTruthy();
    });

    test('getUrl with invalid url', () => {
        expect(false).toBeTruthy();
    });
});